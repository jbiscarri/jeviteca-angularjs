angular      .module("jeviteca")      .service('readerService',['$http', 'settingsValue', function($http, settingsValue) {            this.readDataFromJSONFile = function( filename ) {                  return $http.get(settingsValue.dataFolder + '/'+ filename, {                      cache: true                  });            };        }]);